.DS_Store
node_modules
public
prof
tsconfig.tsbuildinfo
.obsidian
.quartz-cache
private/
.replit
replit.nix

# NOTE: do NOT ignore content/ here. Quartz uses globby with `gitignore: true`,
# so listing it would make the file discovery return zero markdown files.
# content/ is git-ignored via .git/info/exclude instead (per-clone, untracked).
